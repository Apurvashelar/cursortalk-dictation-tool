{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop windows.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "global-shortcut:default"
  ]
}
